#include "main.h"
#include <stdio.h>
/**
* _abs - computes the absolute value of an integer
* @a: is the int that will be used in the function
* return: 0
*/
int _abs(int a)
{
if (a > 0 || a == 0)
{
return (a);
}
else
return (c * -1);
}
