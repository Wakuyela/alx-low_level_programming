#include "main.h"
/**
* _islower - function checks for lower case letters
*@c: is the int value texted in the arguments
* Return: Always 0
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{ return (1);
}
else
return (0);
}
