#include "main.h"
/**
 * print_sign - function checks for value of integer
 *@n: is the int value being checked
 * Return: 0
 */
int print_sign(int n)
{
if (n > 0)
{
_putchar('+');
return (1);
}
else if (n < 0)
{
_puthar('-');
return (-1);
}
else
{
_putchar('0');
return (0);
}
}
